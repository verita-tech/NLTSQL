{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Nltsql": "Debug"
    }
  },

  "DetailedErrors": true,

  "Cube": {
    "BaseUrl": "http://localhost:4000",
    "//": "Must match CUBEJS_API_SECRET from .env. Use user-secrets rather than committing a real value.",
    "ApiSecret": "change-me-cube-api-secret-at-least-32-chars"
  }
}
